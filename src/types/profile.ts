export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateProfileData(profile: ChessProfile): boolean { if (profile.address.length === 0) return false; if (profile.username.length === 0) return false; if (profile.displayName.length > 30) return false; if (profile.bio.length > 120) return false; return true; }