import { NextRequest, NextResponse } from 'next/server' import { Redis } from '@upstash/redis' import type { Abi } from 'viem' import { getPublicClient, type EvmChain } from '@/lib/evm-server' import { syncGameIndex, getIndexedPlayers } from '@/lib/game-index' import { CELO_CONTRACTS, BASE_CONTRACTS } from '@/config/contracts' import { EVM_CHESS_ORACLE_ABI } from '@/config/abis' export const runtime = 'nodejs' export const dynamic = 'force-dynamic' const CACHE_TTL = 20 // seconds — stats change slowly; bounds multicall load export interface LeaderboardEntry { address: string wins: number losses: number draws: number rating: number gamesPlayed: number rank: number } function getRedis(): Redis { return new Redis({ url: process.env.UPSTASH_REDIS_REST_URL!, token: process.env.UPSTASH_REDIS_REST_TOKEN!, }) } function parseEvmChain(value: string | null): EvmChain | null { return value === 'celo' || value === 'base' ? value : null } function gameAddress(chain: EvmChain): `0x${string}` { return (chain === 'celo' ? CELO_CONTRACTS.game : BASE_CONTRACTS.game) as `0x${string}` } function processLeaderboardEntries(statsResults: any[], addresses: string[]): LeaderboardEntry[] { const entries: LeaderboardEntry[] = [] for (let i = 0; i < addresses.length; i++) { const result = statsResults[i] if (result.status !== 'success') continue const r = result.result as readonly [bigint, bigint, bigint, bigint, bigint] const gamesPlayed = Number(r[4]) if (gamesPlayed === 0) continue entries.push({ address: addresses[i], wins: Number(r[0]), losses: Number(r[1]), draws: Number(r[2]), rating: Number(r[3]), gamesPlayed, rank: 0, }) } entries.sort((a, b) => b.rating - a.rating || b.wins - a.wins) entries.forEach((e, i) => { e.rank = i + 1 }) return entries } // GET /api/leaderboard?chain=celo|base — Redis-indexed leaderboard for an EVM // chain. Scans only games created since the last index sync, then reads // playerStats for known players. (Stacks leaderboard reads via @stacks elsewhere.) export async function GET(req: NextRequest) { const chain = parseEvmChain(req.nextUrl.searchParams.get('chain') ?? 'celo') if (!chain) return NextResponse.json({ error: 'invalid chain' }, { status: 400 }) const cacheKey = `chess:idx:${chain}:leaderboard` try { const redis = getRedis() const cached = await redis.get<LeaderboardEntry[]>(cacheKey) if (cached) return NextResponse.json({ entries: cached, cached: true }) await syncGameIndex(chain) const addresses = await getIndexedPlayers(chain) if (addresses.length === 0) return NextResponse.json({ entries: [] }) const statsResults = await getPublicClient(chain).multicall({ contracts: addresses.map((addr) => ({ address: gameAddress(chain), abi: EVM_CHESS_ORACLE_ABI as Abi, functionName: 'playerStats', args: [addr as `0x${string}`], })), allowFailure: true, }) const entries = processLeaderboardEntries(statsResults, addresses) await redis.set(cacheKey, entries, { ex: CACHE_TTL }) return NextResponse.json({ entries }) } catch (err) { console.error('[api/leaderboard] failed:', (err as Error)?.message) return NextResponse.json({ error: 'leaderboard unavailable' }, { status: 503 }) } }}