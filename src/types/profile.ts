export interface ChessProfileTimestamps { createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ChessProfile { address: string; // 0x… (lowercased) for Celo, SP…/ST… (verbatim) for Stacks username: string; // "jadon" — displayed as "jadon.chess" displayName: string; // freeform, max 30 chars bio: string; // max 120 chars og: boolean; // first 100 profiles, locked forever timestamps: ChessProfileTimestamps; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null> }