export interface BaseProfile { address: string; username: string; displayName: string; bio: string; } export interface ChessProfile extends BaseProfile { og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null> }