export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateChessProfile(profile: any): boolean { if (!profile || typeof profile !== 'object') return false; if (!profile.address || typeof profile.address !== 'string') return false; if (!profile.username || typeof profile.username !== 'string') return false; if (!profile.displayName || typeof profile.displayName !== 'string' || profile.displayName.length > 30) return false; if (!profile.bio || typeof profile.bio !== 'string' || profile.bio.length > 120) return false; if (typeof profile.og !== 'boolean') return false; if (typeof profile.createdAt !== 'number' || typeof profile.updatedAt !== 'number' || typeof profile.usernameChangedAt !== 'number') return false; return true; }