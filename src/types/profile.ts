export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateProfile(profile: ChessProfile): boolean { if (profile.displayName.length > 30) return false; if (profile.bio.length > 120) return false; if (!isValidAddress(profile.address)) return false; return true; } function isValidAddress(address: string): boolean { const celoRegex = /^0x[a-f0-9]{40}$/i; const stacksRegex = /^(SP|ST)[0-9a-f]{40}$/i; return celoRegex.test(address) || stacksRegex.test(address); }