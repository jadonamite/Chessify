import { NextResponse } from 'next/server'; import { getRecentProfiles } from '@/lib/profile-store'; const RECENT_PROFILES_LIMIT = 10; const handleGetRecentProfiles = async (limit: number) => { const profiles = await getRecentProfiles(limit); return NextResponse.json({ profiles }); }; export async function GET() { return handleGetRecentProfiles(RECENT_PROFILES_LIMIT); }