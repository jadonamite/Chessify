import { NextResponse } from 'next/server'; import { getRecentProfiles } from '@/lib/profile-store'; const RECENT_PROFILES_LIMIT = 10; export async function GET() { return getRecentProfilesResponse(RECENT_PROFILES_LIMIT); } function getRecentProfilesResponse(limit: number) { return getRecentProfiles(limit).then((profiles) => NextResponse.json({ profiles })); }