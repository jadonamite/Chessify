export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateChessProfile(profile: ChessProfile): boolean { const { address, username, displayName, bio, og, createdAt, updatedAt, usernameChangedAt } = profile; if (!address || typeof address !== 'string') return false; if (!username || typeof username !== 'string') return false; if (!displayName || typeof displayName !== 'string' || displayName.length > 30) return false; if (!bio || typeof bio !== 'string' || bio.length > 120) return false; if (typeof og !== 'boolean') return false; if (typeof createdAt !== 'number' || typeof updatedAt !== 'number' || typeof usernameChangedAt !== 'number') return false; return true; }